# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Excel/CSV handling
openpyxl>=3.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# PDF processing
PyPDF2>=3.0.0

# Statistics
scipy>=1.10.0

# Windows-specific (Outlook integration)
pywin32>=305 ; platform_system=="Windows"

# HTTP requests (for web features if used)
requests>=2.31.0

# Web interface (P4)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.2
aiofiles>=23.2.1
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Note: tkinter is part of Python standard library
# Note: Standard library modules (csv, json, os, etc.) don't need to be listed